[phases.setup]
nixPkgs = ["...", "jdk17"]

[phases.build]
cmds = ["mvn clean package -DskipTests"]

[start]
cmd = "java -Xmx512m -Dserver.port=$PORT -Dspring.profiles.active=production -Dmanagement.server.port=$PORT -jar target/backend-0.0.1-SNAPSHOT.jar"